
import React from 'react';
import SwipeableDiscoveryFlow from '@/components/discovery/SwipeableDiscoveryFlow';

const Discovery = () => {
  return <SwipeableDiscoveryFlow />;
};

export default Discovery;
